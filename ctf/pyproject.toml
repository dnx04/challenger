[project]
name = "ctf"
version = "0.1.0"
description = "CTF Environment"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "netexec",
    "pwntools>=4.14.0",
    "sqlmap>=1.9",
]

[tool.uv.sources]
netexec = { git = "https://github.com/Pennyw0rth/NetExec" }
