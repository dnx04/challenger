#include <bits/stdc++.h>

using namespace std;

#ifdef local
#include "lib/prettyprint.hpp"
#endif

signed main() { 
  ios::sync_with_stdio(false), cin.tie(0);
  int n, m, r;
  cin >> n >> m >> r;

  vector<int> rent, buy;
  for(int i = 0; i < n; ++i){

  }
}